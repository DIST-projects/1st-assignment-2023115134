# Assignment 1: RPC and RMI in Cloud Environment

## Technologies Used
- RPC: Python XML-RPC
- RMI: Java RMI
- Cloud Platform: Microsoft Azure VM

## RPC Implementation
- Server hosted on Azure VM
- Client accesses remote procedures
- Successful remote invocation demonstrated

## RMI Implementation
- Remote interface defined
- Remote object registered with RMI registry
- Client invokes remote methods
- Armstrong number verified remotely

## Execution
- Server executed on Azure VM
- Client executed on local machine
- Output screenshots included


## Result
Successful RPC and RMI communication with correct output.

Note: The Azure VM used for execution was deleted after successful testing.
The server IP in client code is a placeholder for security reasons.
